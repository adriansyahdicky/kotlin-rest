spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_kotlin
    username: postgres
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none

server.port: 8181
